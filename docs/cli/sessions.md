---
summary: "CLI reference for `atomicbot sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `atomicbot sessions`

List stored conversation sessions.

```bash
atomicbot sessions
atomicbot sessions --active 120
atomicbot sessions --json
```
