---
summary: "CLI reference for `atomicbot clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `atomicbot clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `atomicbot clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `atomicbot clawbot qr` (same behavior as [`atomicbot qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `atomicbot clawbot qr` -> `atomicbot qr`
