---
summary: "CLI reference for `atomicbot uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `atomicbot uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
atomicbot uninstall
atomicbot uninstall --all --yes
atomicbot uninstall --dry-run
```
